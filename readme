感想
我一开始准备是用vue写的，可是发现自己确实有很多不太会，所以上网找资料并求教于一入门早的同学，得以完成了vue的todolist，不过还是有一些bug。于是自己用原生JS又写了一份，不过确实是比vue写起来繁琐多了。
(vue版)功能如下:
  1.按回车添加新todo；
  2.点按钮筛选全部、已完成、未完成；
  3.点排序图标可以进行升序、降序排序；排序默认是时间降序，优先级降序。矛盾的情况下，优先级>时间；
  4.点击"-"可以实现todo的删除；
  
 (JS版)功能如下:
  1.按回车添加新todo；
  2.点按钮筛选全部、已完成、未完成；
  3.选择优先级，可以使todo提前；
  4.点击"delete"可以实现todo的删除；
  5.使用localStorage记录添加的todos，重新加载页面时显示之前添加的todos；
